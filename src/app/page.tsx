export default function HomePage() {
  return (
    <main className="min-h-screen bg-panel px-6 py-8 text-ink">
      <section className="mx-auto flex max-w-6xl flex-col gap-3">
        <p className="text-sm font-semibold uppercase tracking-wide text-accent">
          Command Center
        </p>
        <h1 className="text-4xl font-semibold">ProfitPilot</h1>
        <p className="max-w-2xl text-base text-slate-600">
          Daily arbitrage operations, profit approvals, supplier risk, and listing automation in one workspace.
        </p>
      </section>
    </main>
  );
}
